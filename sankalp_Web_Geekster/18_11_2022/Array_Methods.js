// Length --> It will give the count of elements

// Add Element to last position push()
// To add the value in an array we have predefined function called push() it will add in last index
// Example arr.push(50) it will add at last position

var arr = [10,20,30,40,90,80,40,40,40];
// console.log(arr);
// arr.push(50);
// arr.push(60);
// arr.push(70, 80); // We Can Add Multiple values also
// console.log(arr);

// Add Element to first Position predefined fun unshift
// It will add new Element in the first position
// Example arr.unshift(5);

// arr.unshift(5);
// console.log(arr);
// arr.unshift(1,2);
// console.log(arr);

// What is predefined and user defined function
// predefined means its logic is already defined
// user defined fun are those which are defined by user its logic

// To remove first element from the array shift()
// arr.shift();
// console.log(arr);

// To remove last element from array pop()
// arr.pop();
// console.log(arr);

// Access Specific Element From Array
// arr[index] -- >syntax

// To print the indexnumber of element
// indexOf() --> // it will return the index of element, If element is not present then it will return -1

// It will return the index which is first occurring
// console.log(arr.indexOf(40));

// It will return the index which is last occuring
// console.log(arr.lastIndexOf(40));

//Includes() whether certain element is present inside the Element or not
// Syntax arr.includes(element) --> True or False

// foreach()
// Syntax arr.foreach(fun(elemnt,index))